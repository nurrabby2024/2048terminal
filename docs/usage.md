# Usage

## Install

```bash
pip install -r requirements.txt
```

## Run

```bash
python -m 2048terminal --help
```

## Configure

Create a config file (JSON or YAML) and point the tool at it:

```bash
python -m 2048terminal run --config config.json
```